{
  "name": "document-audit-manager",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm --workspace backend run dev\" \"npm --workspace frontend run dev\"",
    "start": "npm --workspace backend start",
    "bootstrap": "npm --workspace backend run bootstrap"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
